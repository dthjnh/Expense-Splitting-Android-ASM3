<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Background color (white) -->
    <stroke
        android:width="1dp"
        android:color="#CCCCCC" /> <!-- Border color (light gray) -->
    <corners android:radius="8dp" /> <!-- Rounded corners -->
    <padding
        android:left="12dp"
        android:right="12dp"
        android:top="8dp"
        android:bottom="8dp" />
</shape>